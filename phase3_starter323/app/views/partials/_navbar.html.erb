<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
      <a class="navbar-brand" href="">A&M Creamery</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        
        <% if logged_in? %>
        <!-- HOME PAGE LINK -->
        <li><%= link_to "Home", home_path %></li>
        
        
        <!-- STORES PAGE LINK -->
        <% if current_user.role? :employee %>
          <li><%= link_to "Stores",stores_path %></li>
        <% else %>  
          <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="stores#index">Stores
          <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "View Stores",stores_path %></li>
            <% if current_user!=nil && (current_user.role? :admin)%>
            <li><%= link_to "Add Store",new_store_path %></li>
            <% end %>
          </ul>
          </li>
      <%end%>
      
      <!-- EMPLOYEES PAGE LINK -->
      <% if current_user!=nil && ((current_user.role? :manager) || (current_user.role? :admin))%>
        <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="employees#index">Employees
        <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to "View Employees",employees_path %></li>
          <li><%= link_to "Add Employee",new_employee_path %></li>
        </ul>
        </li>
      <% end %>
     
      
    
      
      <!-- ASSIGNMENTS PAGE LINK -->
      <% if current_user.role? :employee %>
          <li><%= link_to "Assignment", assignments_path %></li>
      <% else %>
      <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="assignments#index">Assignments
      <span class="caret"></span></a>
      <ul class="dropdown-menu">
        <li><%= link_to "View Assignments",assignments_path %></li>
        <% if current_user!=nil && (current_user.role? :admin)%>
          <li><%= link_to "Add Assignment",new_assignment_path %></li>
        <% end %>
      </ul>
      </li>
      <% end %>
      
      <!-- SHIFTS PAGE LINK -->
      <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="shifts#index">Shifts
      <span class="caret"></span></a>
      <ul class="dropdown-menu">
        <li><%= link_to "View Shifts",shifts_path %></li>
        <li><%= link_to "Complete Shifts", shifts_path(shift_type: "complete") %></li>
        <li><%= link_to "Incomplete Shifts", shifts_path(shift_type: "incomplete") %></li>
        <li><%= link_to "Upcoming Shifts", shifts_path(shift_type: "upcoming") %></li>
        <li><%= link_to "Past Shifts", shifts_path(shift_type: "past") %></li>
        
        <% if current_user!=nil && ((current_user.role? :manager) || (current_user.role? :admin))%>
          <li><%= link_to "Add Shift", new_shift_path %></li>
        <% end %>
      </ul>
      </li>
      
      <!-- JOBS PAGE LINK -->
      
      <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="jobs#index">Jobs
      <span class="caret"></span></a>
      <ul class="dropdown-menu">
        <li><%= link_to "View Jobs",jobs_path %></li>
        <% if current_user!=nil && (current_user.role? :admin)%>
          <li><%= link_to "Add Job", new_job_path %></li>
        <% end %>
      </ul>
      </li>
      
      <!-- FLAVORS PAGE LINK -->
      <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="flavors#index">Flavors
      <span class="caret"></span></a>
      <ul class="dropdown-menu">
        <li><%= link_to "View Flavors",flavors_path %></li>
        <% if current_user!=nil && (current_user.role? :admin)%>
            <li><%= link_to "Add Flavor", new_flavor_path %></li>
        <% end %>
      </ul>
      </li>
      
     
     <% else %>
     
      <li><%= link_to "Home", home_path %></li>
      <li><%= link_to "Stores",stores_path %></li>
        
      </ul>
      <% end%>
      
      <% if logged_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Logout",logout_path %></li>
        </ul>
      <% else %>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Login",login_path %></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
